// calculate discount

// function discount(totalValue,discountPercentage){
//    let discountPrice=((totalValue*discountPercentage)/100)
//     return discountPrice;
// }
let totalValue=2000;
let discountPercentage=20;

let discountPrice=(totalValue*(discountPercentage/100));
let finalPrice=(totalValue-discountPrice)
console.log("The final price after discount is RS. "+ (finalPrice));

// output:The final price after discount is RS. 1600